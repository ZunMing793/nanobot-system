# 知识库

> 本目录用于存放 bot 可复用的外部资料,与记忆/经验文件分层管理。

## 目录说明

- `raw/`:原始资料,如 `pdf`、`docx`、`epub`
- `extracted/`:从原始资料中提取出的 `md/txt`
- `index/`:索引、摘要、标签、清单

## 使用原则

- 优先把原始文件放入 `raw/`
- 优先读取 `index/` 中的清单和摘要
- 需要时再调用相应 skills 解析原文件
- 解析结果应尽量缓存到 `extracted/`
